# Platform Specific Implementations

This directory contains platform-specific implementations for video processing.

## Structure

- `base/` - Base classes and interfaces
- `macos/` - macOS specific implementations
  - `network/` - Network and download utilities
  - `video/` - Video processing utilities
  - `platforms/` - Platform specific video handlers
- `interface/` - Interface definitions for platform communication